# requirements.txt

# Streamlit
streamlit

# LangChain 핵심 (upstage 0.1.5와 호환되는 버전으로 고정)
langchain==0.1.20
langchain-core==0.1.52
langchain-community==0.0.38
langchain-text-splitters==0.0.2

# Upstage (langchain-openai<0.2.0 을 내부적으로 요구)
langchain-upstage==0.1.5
langchain-openai>=0.1.3,<0.2.0   # ← upstage가 요구하는 범위로 고정

# Pinecone
langchain-pinecone==0.1.0
pinecone==7.3.0

# 기타
python-dotenv
openai>=1.0.0,<2.0.0
tiktoken